import 'package:flutter/material.dart';

// DashboardTab : onglet 1 - tableau de bord avec statistiques
// Pour l'instant c'est un placeholder, on le remplira après
class DashboardTab extends StatelessWidget {
  const DashboardTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Dashboard - En construction'),
    );
  }
}