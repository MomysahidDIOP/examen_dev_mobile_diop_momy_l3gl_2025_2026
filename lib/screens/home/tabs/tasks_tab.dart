import 'package:flutter/material.dart';

// TasksTab : onglet 3 - liste des tâches
class TasksTab extends StatelessWidget {
  const TasksTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tâches - En construction'),
    );
  }
}